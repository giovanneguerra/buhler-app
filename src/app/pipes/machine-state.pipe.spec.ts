import { MachineStatePipe } from './machine-state.pipe';

describe('MachineStatePipe', () => {
  it('create an instance', () => {
    const pipe = new MachineStatePipe();
    expect(pipe).toBeTruthy();
  });
});
